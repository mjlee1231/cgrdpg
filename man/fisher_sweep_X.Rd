% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fisher.R
\name{fisher_sweep_X}
\alias{fisher_sweep_X}
\title{One Fisher-scoring sweep updating X}
\usage{
fisher_sweep_X(
  A,
  X,
  Z,
  B,
  sign_diag,
  tau = 0.05,
  ridge = 0,
  w_cap = Inf,
  ls_beta = 0.8,
  ls_c = 0.01,
  ls_max = 20
)
}
\arguments{
\item{A}{(n x n) symmetric adjacency matrix.}

\item{X}{(n x d) current embedding.}

\item{Z}{(p_cov x d) current Z matrix.}

\item{B}{(p_cov x n) covariate matrix with columns corresponding to nodes.}

\item{sign_diag}{(d x d) diagonal signature matrix with +1 (p) and -1 (q).}

\item{tau}{smoothing parameter for psi.}

\item{ridge}{small ridge added to the information matrix for numerical stability.}

\item{w_cap}{optional cap on psi'(s); default Inf}

\item{ls_beta}{backtracking shrink factor in (0,1); default 0.8.}

\item{ls_c}{Armijo constant in (0,1); default 1e-2 (for ascent).}

\item{ls_max}{maximum backtracking steps; default 20.}
}
\value{
Updated X.
}
\description{
Implements the per-node Fisher-scoring update for the surrogate log-likelihood
described in the model, fixing \eqn{Y} and \eqn{Z} while updating each row \eqn{x_i}
with an Armijo backtracking line search (maximization).
}
